# DETR_Transformer_Model
Reproduced DETR (Detection Transformer) from scratch in PyTorch — implementing ResNet-50 backbone, sinusoidal 2D positional encodings, transformer encoder-decoder, Hungarian bipartite matching, and GIoU loss. Trained on COCO for 80 epochs, achieving a 52% loss reduction. Built as a research foundation.
